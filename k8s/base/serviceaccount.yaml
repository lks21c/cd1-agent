apiVersion: v1
kind: ServiceAccount
metadata:
  name: cd1-agent
  namespace: cd1-agent
  labels:
    app.kubernetes.io/name: cd1-agent
    app.kubernetes.io/component: serviceaccount
  annotations:
    # For AWS IRSA (IAM Roles for Service Accounts)
    # Replace with your actual IAM role ARN
    eks.amazonaws.com/role-arn: "arn:aws:iam::ACCOUNT_ID:role/cd1-agent-role"
